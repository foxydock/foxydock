server {
    listen 0.0.0.0:27892 proxy_protocol;
    listen [::]:27892    proxy_protocol;

    # 启用SSL预读，不解密流量，只提取 SNI
    ssl_preread on;
    access_log /var/log/nginx/stream_access.log proxy buffer=256m flush=2s;

    proxy_protocol on;
    proxy_pass $fdn_upstream;
}
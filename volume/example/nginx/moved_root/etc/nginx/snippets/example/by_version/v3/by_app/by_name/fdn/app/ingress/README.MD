# ingress 网关

* `mix_app_ingress_map.conf` 工作于 `ngx_http_core_module` 的 `http` 的上下文  
* `mix_default_server.conf` 工作于 `ngx_http_core_module` 的 `server` 的上下文  
* `mix_server_common_setting.conf` 工作于 `ngx_http_core_module` 的 `server` 的上下文  

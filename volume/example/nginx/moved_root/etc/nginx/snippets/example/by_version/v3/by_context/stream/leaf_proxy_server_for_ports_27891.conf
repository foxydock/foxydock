server {
    listen 0.0.0.0:27891 proxy_protocol;
    listen [::]:27891 proxy_protocol;

    access_log /var/log/nginx/stream_access.log proxy buffer=256m flush=2s;

    proxy_protocol on;
    proxy_pass unix:/var/run/nginx/fdn_ingress_server.sock;
}